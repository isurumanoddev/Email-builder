import { BuilderGallery } from '@/builder/components/BuilderGallery';

export const metadata = {
  title: 'Template Builder | React Email',
  description: 'Create and manage email templates',
};

export default function BuilderPage() {
  return <BuilderGallery />;
}
